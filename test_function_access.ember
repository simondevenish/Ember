// Test accessing function properties
print("=== Function Access Test ===");

var obj = {
    name: "Test",
    greet: fn() {
        print("Hello from function!");
    }
};

print("Object name: " + obj.name);

// Try to access the function property (but not call it)
print("Function property exists: " + (obj.greet != null));

print("=== Test completed ==="); 