// Testing nested objects with manual traversal

// Create a simple nested object
var obj = {
    x: {
        y: "original"
    }
};

// Print initial value
print("Initial obj.x.y: " + obj.x.y);

// Update nested property manually
var inner = obj.x;  // Get inner object
inner.y = "updated";  // Update property on inner object
obj.x = inner;  // Set inner object back into outer object

// Verify the update
print("Updated obj.x.y: " + obj.x.y); 