// Minimal test for direct nested property assignment
var obj = {
    x: { 
       y: "original"
    }
};

print("Before: obj.x.y = " + obj.x.y);

// Use direct nested property assignment
obj.x.y = "updated";

print("After: obj.x.y = " + obj.x.y); 