// Test function expressions in objects
print("=== Testing Function Expressions ===");

// Test 1: Simple function expression in object
var obj1 = {
    name: "TestObject",
    greet: fn() {
        print("Hello from object!");
    }
};

print("obj1.name: " + obj1.name);
// obj1.greet();  // We'll test this after implementing 'this' context

// Test 2: Function with parameters
var obj2 = {
    add: fn(a, b) {
        print("Adding: " + a + " + " + b);
    }
};

// obj2.add(5, 3);  // We'll test this after implementing 'this' context

print("=== Function expression test completed ==="); 