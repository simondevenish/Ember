// Simple nested property test with manual updating

// Create a simple nested object
var obj = {
    x: {
        y: "original"
    }
};

// Print initial value
print("Initial obj.x.y: " + obj.x.y);

// Get the inner object
var inner = obj.x;

// Update its property
inner.y = "updated";

// Set it back to the original object
obj.x = inner;

// Verify the update
print("Updated obj.x.y: " + obj.x.y); 